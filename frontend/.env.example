# 接口基址：开发 / npm run preview 留空 = 走 Vite 代理（推荐）
# 生产静态站：与页面同源且 Nginx 反代 /api 时可留空；否则填后端根地址，如 http://127.0.0.1:3001（不要写 .../api）
VITE_API_BASE_URL=

# 仅开发有效：Vite 把 /api 转发到该地址（默认 http://127.0.0.1:3001）
# VITE_PROXY_TARGET=http://127.0.0.1:3001
